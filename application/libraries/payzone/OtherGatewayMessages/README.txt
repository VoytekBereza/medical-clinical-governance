These files show how to call the other gateway messages:
	CrossReferenceTransacion
	GetCardType

They do not form part of the example payment form, so they have been put in this subfolder so as not to confuse the sample payment form.